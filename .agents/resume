#!/usr/bin/env bash
set -euo pipefail

for name in UNIVERSE_API_BASE_URL; do
	if [[ -z "${!name:-}" ]]; then
		printf -v "$name" '%s' "$(amp secrets get "$name" --project)"
		export "$name"
	fi
done

amp orb services ensure
