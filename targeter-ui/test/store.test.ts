import test from 'node:test';import assert from 'node:assert/strict';import {Readable} from 'node:stream';import {S3ArchiveStore} from '../src/server/store.js';
test('S3 pagination and ExpectedBucketOwner on list/get',async()=>{const calls:any[]=[];const client:any={send:async(c:any)=>{calls.push(c.input);if(c.constructor.name==='ListObjectsV2Command')return c.input.ContinuationToken?{Contents:[{Key:'p/b/run_manifest.json'}]}:{IsTruncated:true,NextContinuationToken:'n',Contents:[{Key:'p/a/run_manifest.json'},{Key:'p/nope'}]};return {ContentLength:2,Body:Readable.from([Buffer.from('ok')])}}};const s=new S3ArchiveStore(client,'bucket','p','owner');assert.equal((await s.listManifests()).length,2);assert.equal(Buffer.from(await s.get('p/a',10)).toString(),'ok');assert.ok(calls.every(x=>x.ExpectedBucketOwner==='owner'))});
